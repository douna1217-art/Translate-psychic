# 注意：这里不再用 VITE_ 前缀！
# VITE_ 开头的变量会被打包进浏览器能看到的前端代码，等于把 Key 公开给所有人。
# ZHIPU_API_KEY 只会在服务器端（api/ai.js）读取，浏览器拿不到。
# 去 https://bigmodel.cn 注册智谱开放平台账号，实名认证后可以领取免费的 GLM-4-Flash Key。
ZHIPU_API_KEY=你的真实key

# 下面两个用 VITE_ 前缀是故意的：Supabase 的 URL 和 anon/publishable key
# 本来就是设计给浏览器用的，安全性由 Supabase 数据库的 Row Level Security 规则保证，
# 不是靠这两个值保密（跟上面的 ZHIPU_API_KEY 性质完全不同，不要混淆）。
VITE_SUPABASE_URL=https://你的项目.supabase.co
VITE_SUPABASE_ANON_KEY=你的anon或publishable key
